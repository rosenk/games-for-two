#!/usr/bin/env bash
set -euo pipefail

echo "Checking the Svelte game workspace..."
command -v node >/dev/null
command -v npm >/dev/null

test -f index.html
test -f package.json
npm ci
npm run check

echo "Orb setup complete."
