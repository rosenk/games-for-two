#!/usr/bin/env bash
set -euo pipefail

echo "Checking the static game workspace..."
command -v node >/dev/null
command -v python3 >/dev/null

test -f index.html
test -f styles.css
test -f app.js
node --check app.js

echo "Orb setup complete. No dependencies to install."
