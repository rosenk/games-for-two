services:
  games-for-two:
    command: npm run dev -- --host 0.0.0.0 --port "$PORT"
    health: /
    agent: control
    portal:
      url: /
      title: Игри за двама
      description: Преглед и тест на играта.
